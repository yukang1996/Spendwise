<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="my.edu.um.fsktm.spendwise">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/spendwise"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AddTransaction"></activity>
        <activity android:name=".AddTransactionIncome"></activity>
        <activity android:name=".AddBudget"></activity>
        <activity android:name=".UpdateTransaction"></activity>
        <activity android:name=".CashFlow"
            android:screenOrientation="landscape"></activity>
    </application>

</manifest>